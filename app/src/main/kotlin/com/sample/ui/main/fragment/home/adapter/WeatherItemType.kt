package com.sample.ui.main.fragment.home.adapter

/**
 * To get different type of items in weather scree, so that we can differentiate easily with different
 * layout, in future we can add multiple types items in same screen as we used recycler view
 */
enum class WeatherItemType {
    TYPE_ITEM
}
